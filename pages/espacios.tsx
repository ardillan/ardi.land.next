import React from "react";

import Head from "next/head";
import Layout from "@/components/Layout";

const Espacios = (): JSX.Element => {
  return (
    <Layout>
      <h1>Mis espacios</h1>
    </Layout>
  );
};

export default Espacios;
